import Foundation

enum AppConstants {
    static let urlScheme = "tokenwidget"
    static let bundleApp = "com.adi.token-widget"
    static let keychainService = "com.adi.token-widget.credentials"
}
